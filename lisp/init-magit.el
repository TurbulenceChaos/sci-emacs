;;; init-magit.el --- Magit config
;;; Commentary:
;;; Code:

(require 'dash)
(require 'transient)
(require 'with-editor)
(require 'llama)
(require 'magit)
(require 'diff-hl)

(global-diff-hl-mode)


(provide 'init-magit)
;;; init-magit.el ends here