;;; Record configuration

;; Keycast mode
(add-to-list 'load-path "~/.emacs.d/lisp-site/keycast")
(require 'keycast)

;; OBS record
;; (keycast-mode-line-mode)
;; (global-set-key (kbd "C-M-a") #'ignore)
;; (global-set-key (kbd "C-M-s") #'ignore)


(provide 'record-config)
